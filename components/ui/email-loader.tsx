"use client";

import React from "react";
import styles from "@styles/email-loading.module.css";

const EmailLoader: React.FC = () => {
  return <span className={styles.loader}></span>;
};

export default EmailLoader;
